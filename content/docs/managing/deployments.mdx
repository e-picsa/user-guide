---
title: Deployments
description: View all country apps registered in the dashboard (admin).
---

**Deployments** (requires `deployments.admin`) lists every app deployment. Day-to-day you switch between them from the header picker or your profile — this page is the registry overview.

<Screenshot
  src="/screenshots/zm-admin/deployment.png"
  alt="Deployments list"
  markers={[
    { x: 30, y: 30, label: 'Country Code and Label — e.g. zm / Zambia App, mw / Malawi App' },
    { x: 60, y: 30, label: 'Public and Restricted flags — visibility and access-key protection' },
    { x: 80, y: 30, label: 'Icon — the deployment’s identifying mark' },
  ]}
  caption="Deployments registry (4 deployments)"
/>

To grant people access to a deployment or change their roles, go to [User Permissions](./user-permissions).
